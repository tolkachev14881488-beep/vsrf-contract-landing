# Лендинг «Служба по контракту в ВС РФ» — передача заказчику

**Домен:** https://sluzhbarf.ru  
**Почта для заявок:** rodionova61@bk.ru  
**Телефон на сайте:** +7 906 310-16-33  

---

## Состав архива

| Папка / файл | Назначение |
|--------------|------------|
| `сайт/` | Готовый статический сайт (HTML, CSS, JS, картинки) — можно залить на любой хостинг |
| `backend/` | Серверная часть (Python Flask) — опционально, для API и резервной почты |
| `docs/DOMAIN.md` | Настройка DNS на Reg.ru |
| `.github/` | Автопубликация на GitHub Pages (если используете GitHub) |

---

## Быстрый старт (только сайт)

1. Загрузите содержимое папки **`сайт/`** на хостинг (корень домена `sluzhbarf.ru`).
2. Убедитесь, что открываются:
   - https://sluzhbarf.ru/
   - https://sluzhbarf.ru/static/css/style.css
3. Отправьте тестовую заявку — письмо придёт на **rodionova61@bk.ru** (сервис Web3Forms).

### Форма заявок (Web3Forms)

Ключ уже прописан в `сайт/index.html`:

```html
<meta name="web3forms-access-key" content="0e707139-1ae5-4f36-b385-cdc7dd07b968">
```

Сменить почту: зарегистрируйте новый ключ на https://web3forms.com и замените значение `content`.

---

## DNS (Reg.ru) для sluzhbarf.ru

Четыре A-записи для `@`:

- `185.199.108.153`
- `185.199.109.153`
- `185.199.110.153`
- `185.199.111.153`

Для `www` (по желанию): CNAME → `tolkachev14881488-beep.github.io`

Подробнее: `docs/DOMAIN.md`

---

## GitHub Pages (текущая публикация)

Репозиторий: `tolkachev14881488-beep/vsrf-contract-landing`  
Ветка: `master`  
После push сайт обновляется автоматически (Actions).

В **Settings → Pages → Custom domain:** `sluzhbarf.ru`  
Включить **Enforce HTTPS**.

---

## Локальный просмотр

Откройте `сайт/index.html` в браузере или запустите простой сервер:

```bash
cd сайт
python -m http.server 8080
```

Откройте http://127.0.0.1:8080

---

## Backend (опционально)

```bash
cd backend
pip install -r requirements.txt
python app.py
```

Сайт: http://127.0.0.1:5000  
API заявок: POST `/api/apply`

Переменные окружения — см. `backend/.env.example`

---

## Редактирование контента

| Что менять | Файл |
|------------|------|
| Тексты, блоки, телефон | `сайт/index.html` |
| Цвета, вёрстка | `сайт/static/css/style.css` |
| Форма, маска телефона | `сайт/static/js/main.js` |
| Фон hero | `сайт/static/images/hero.jpg` |
| Фото в блоке «Условия» | `сайт/static/images/otbor-2026.png` |
| SEO, заголовок вкладки | `<head>` в `index.html` |

---

## Важно

Сайт **не является официальным ресурсом Минобороны РФ** (указано в подвале).  
Для рекламы используйте согласованные формулировки.

---

## Контакты разработки

Исходный проект: папка `vsrf` / репозиторий GitHub `vsrf-contract-landing`  
Дата сборки пакета: июнь 2026
